/**
 * Here we store all the routes so that
 * we can manage them all from one place
 */
const Routes = Object.freeze({
  Home: '/',
  Profile: '/profile',
  Logs: '/profile/logs',
  About: '/about',
})

export { Routes }
